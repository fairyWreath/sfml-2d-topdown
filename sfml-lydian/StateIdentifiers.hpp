#ifndef STATEIDENTIFIERS_HPP
#define STATEIDENTIFIERS_HPP

// identifiers for (screen) states
namespace States
{
	enum ID
	{
		None,
		Title,
		Menu,
		Settings,
		Config,
		Loading,
		Pause,
		Game,
		GameEnd
	};
}


#endif